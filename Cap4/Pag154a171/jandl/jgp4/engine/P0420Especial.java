package jandl.jgp4.engine;

public interface P0420Especial {
	/* → INTERFACE DE MARCAÇÃO ← 
	
	As interfaces de marcação em Java não exigem a presença de métodos ou constantes. 
	Elas podem ser criadas sem a presença de nenhum elemento.

	Interfaces de marcação são usadas como um mecanismo para aplicar uma "marca" ou 
	"rótulo" a uma classe, indicando que ela segue certas regras ou contratos. Por 
	exemplo, uma classe que implementa a interface Serializable pode ser marcada como 
	sendo passível de serialização.

	Em resumo, uma interface de marcação pode ser criada sem a presença de métodos 
	ou constantes, e sua principal função é marcar ou rotular as classes que a implementam.

	As interfaces de marcação em Java são consideradas "vazias" no sentido de que elas 
	não têm a obrigação de ter métodos ou constantes definidos nelas. No entanto, elas 
	ainda são contratos ou regras que devem ser seguidas pelas classes que as implementam.

	Quando uma classe implementa uma interface de marcação, ela está declarando que segue 
	as regras ou contratos associados àquela interface. Mesmo que a interface não tenha 
	nenhum método ou constante definido nela, ela ainda é um contrato que a classe que a 
	implementa deve seguir.

	Por exemplo, uma classe que implementa a interface Serializable está declarando que 
	é passível de serialização, mesmo que a interface Serializable não tenha nenhum método 
	ou constante definido nela. A classe que implementa a interface está seguindo o contrato 
	associado à interface de ser passível de serialização.

	Em resumo, mesmo que as interfaces de marcação não tenham métodos ou constantes 
	definidos nelas, elas ainda são consideradas contratos ou regras que as classes 
	que as implementam devem seguir. */
}
